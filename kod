k <- 10 # rozmiar macierzy (później = 24)
A <- matrix(NA, k, k)
A[k, 1] <- 1
for (i in 2:k){
  for (j in (k - i + 1):k){
    if (j == k) {
      A[j, i] <- d * A[j, i - 1]
    }
    else {
      A[j, i] <- u * A[j + 1, i - 1]
    }
  }
}
